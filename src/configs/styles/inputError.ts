import colors from 'tailwindcss/colors';

const inputError: React.CSSProperties = {
    borderColor: colors.rose[700],
    outlineColor: colors.rose[700],
    color: colors.rose[700],
}

export default inputError;